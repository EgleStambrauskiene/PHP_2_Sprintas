<?php
namespace ConfigClass;

class ConfigClass
{
    const DB_DRIVER = 'mysql';
    const DB_HOST = 'localhost';
    const DB_USER = 'web_staff';
    const DB_DATA = 'staff';
    const DB_PORT = 3306;
    const DB_PASS = 'zebras';
    // define('BASE_URL',  $httpRoot . $httpDir);
    // define('HTTP_PATH', $path);
    // define('ROOT_DIR', $rootDir);
}

?>